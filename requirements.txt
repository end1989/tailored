# Exact versions the test suite passes on. Unpinned installs broke the app:
# newer sqlmodel rejects the naive UTC datetimes the models store, and
# anthropic 1.x changed the client. Bump a pin only after the full suite
# (pytest, including the pdf marker) passes on the new version.
fastapi==0.141.1
uvicorn[standard]==0.52.4
sqlmodel==0.0.39
pydantic==2.13.4
anthropic==0.125.0
httpx==0.28.1
trafilatura==2.2.0
jinja2==3.1.6
markdown==3.10.3
playwright==1.62.0
mcp==2.0.0
pypdf==6.16.1
python-docx==1.2.0
python-multipart==0.0.32
python-dotenv==1.2.3
pytest==9.1.1
respx==0.23.1
# Imported directly by the code, so pinned here rather than left to drift in
# as someone else's dependency.
anyio==4.14.2
sqlalchemy==2.0.52
starlette==1.6.0
